;;; undo-tree-config --- Config for ELPA installed packaged
;;; Commentary:
;;;
;;; Config for undo-tree package

;;; Code:

(require 'undo-tree)
(require 'diminish)
(global-undo-tree-mode)
(diminish 'undo-tree-mode)

;;; undo-tree-config.el ends here
