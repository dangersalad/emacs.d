;;; helm-tramp-config --- Config for ELPA installed packaged
;;; Commentary:
;;;
;;; Config for helm-tramp package

;;; Code:

(require 'helm-tramp)
(define-key global-map (kbd "C-c s") 'helm-tramp)

;;; helm-tramp-config.el ends here
