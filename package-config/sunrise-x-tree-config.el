;;; sunrise-x-tree-config --- Config for ELPA installed packaged
;;; Commentary:
;;;
;;; Config for sunrise-x-tree package

;;; Code:

(require 'sunrise-x-tree)

(setq tree-widget-image-enable nil)

;;; sunrise-x-tree-config.el ends here
