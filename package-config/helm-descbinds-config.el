;;; helm-descbinds-config --- Config for ELPA installed packaged
;;; Commentary:
;;;
;;; Config for helm-descbinds package

;;; Code:

(require 'helm-descbinds)
(global-set-key (kbd "C-h b") 'helm-descbinds)

;;; helm-descbinds-config.el ends here
