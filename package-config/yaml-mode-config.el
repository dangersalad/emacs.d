;;; yaml-mode-config --- Config for ELPA installed packaged
;;; Commentary:
;;;
;;; Config for yaml-mode package

;;; Code:

(require 'yaml-mode)
(add-to-list 'auto-mode-alist '("\\.yaml\\'" . yaml-mode))

;;; yaml-mode-config.el ends here
